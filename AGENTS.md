# Agent guidelines

- Keep changes focused and consistent with existing logging style.
- Prefer explicit exception handling (avoid bare except).
- Update README when behavior or configuration changes.
- Run tests only when explicitly requested or when changes are risky.
